import { StyleSheet } from "react-native";

export default StyleSheet.create({
    participant: {
        flex: 1,
        backgroundColor: "#1F1E25",
        width: "100%",
        height: 60,
        flexDirection: "row",
        alignItems: "center",
        padding: 16,
      },
    button: {
        width: 56,
        height: 60,
        backgroundColor: "#E23C44",
        alignItems: "center",
        justifyContent: "center",
        borderRadius: 4,
      },
    buttonMinus: { fontSize: 20, color: "#FFF" }
})